# Q pre block. Keep at the top of this file.
[[ -f "${HOME}/Library/Application Support/amazon-q/shell/zprofile.pre.zsh" ]] && builtin source "${HOME}/Library/Application Support/amazon-q/shell/zprofile.pre.zsh"
#!/bin/sh

# Set PATH, MANPATH, etc., for Homebrew.
#
# Guarded for the same reason as in .zshrc: chezmoi writes this file during the
# very first apply, and a login shell started before Homebrew is installed would
# otherwise error. provision.sh deliberately does *not* append its own copy of
# this line -- chezmoi owns this file and would overwrite the append anyway.
[[ -x /opt/homebrew/bin/brew ]] && eval "$(/opt/homebrew/bin/brew shellenv)"

# Initialize asdf in .zprofile (instead of .zshrc) so that asdf is available in
# all shells, including non-interactive ones.
if [[ -d "/opt/homebrew/opt/asdf/bin" ]]; then
  export ASDF_DATA_DIR="$HOME/.asdf"
  export PATH="/opt/homebrew/opt/asdf/bin:$ASDF_DATA_DIR/shims:$PATH"
fi

# Set PATH for python programs installed with pip
PATH="$HOME/.local/bin:$PATH"
export PATH

# git() {
#   if [[ "$1" == "clone" ]]; then
#     shift  # Remove 'clone' from arguments
#     git-clone "$@"
#   else
#     command git "$@"
#   fi
# }

# Q post block. Keep at the bottom of this file.
[[ -f "${HOME}/Library/Application Support/amazon-q/shell/zprofile.post.zsh" ]] && builtin source "${HOME}/Library/Application Support/amazon-q/shell/zprofile.post.zsh"
